const listControllers = require('./list-controllers')
const randomColor = require('./random-color')
const randomInterval = require('./random-interval')
const setColor = require('./set-color')

module.exports = {
  listControllers, randomColor, randomInterval, setColor,
}
